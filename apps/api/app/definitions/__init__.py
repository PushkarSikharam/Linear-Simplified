"""Product Definitions: the reusable, versioned description of a product that Pixel core runs.

Nothing in this package may name a specific product. Product semantics live in
`products/<product_id>/definition/v<N>.yaml`; this package only loads, validates, versions
and binds them.
"""
