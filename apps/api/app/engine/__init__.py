"""Pixel's generic conversation engine (Milestone 3, step 3.2).

Every decision here comes from the session's pinned Product Definition. Modules in this
package must not depend on product-specific code, directly or indirectly; the dependency
boundary test in `apps/api/tests/test_engine_boundary.py` enforces this.
"""
